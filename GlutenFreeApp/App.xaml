<?xml version = "1.0" encoding = "UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:GlutenFreeApp"
             x:Class="GlutenFreeApp.App">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles/Colors.xaml" />
                
                <ResourceDictionary Source="Resources/Styles/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            
            <Style x:Key="buttonStyle" TargetType="Button">
                <Setter Property="HorizontalOptions" Value="Fill" />
                <Setter Property="BackgroundColor" Value="Purple" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="Margin" Value="10" />
            </Style>

            <Style x:Key="DefaultEntry" TargetType="Entry">
                <Setter Property="PlaceholderColor" Value="{AppThemeBinding Light=#6c757d, Dark=#adb5bd}" />
                <Setter Property="TextColor" Value="{AppThemeBinding Light=#212529, Dark=White}" />
                <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#f8f9fa, Dark=#495057}" />
                <Setter Property="HeightRequest" Value="50" />
                
                <Setter Property="VerticalTextAlignment" Value="Center" />
                <Setter Property="Margin" Value="0,5" />
            </Style>

        
        </ResourceDictionary>
    </Application.Resources>
</Application>
